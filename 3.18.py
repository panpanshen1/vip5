#字符串操作
la = '   safsadfdsfdss    DDDdfdsfd   ssadfds       '
lb = 'sdfsdfds'
#print(la.capitalize())
# print(la.count('s',0,5))
# print(la.encode('UTF-8', 's'))
# print(la.endswith('s',0,6))
#print(la.find('s',0,100))
#检测 str 是否包含在 string 中，如果 beg 和 end 指定范围，则检查是否
#包含在指定范围内，如果是返回开始的索引值，否则返回-1
# print(la.index('y',0,5))
#跟find()方法一样，只不过如果str不在 string中会报一个异常.
# print(la.isalpha())
# 如果 string 至少有一个字符并且所有字符都是字母则返回 True,
# 否则返回 False
# print(la.isdigit())
# 如果 string 只包含数字则返回 True 否则返回 False.
# print(la.islower())
# 如果 string 中包含至少一个区分大小写的字符，并且所有这些
# (区分大小写的)字符都是小写，则返回 True，否则返回 False
# print(la.isnumeric())
# 如果 string 中只包含数字字符，则返回 True，否则返回 False
# print(la.isupper())
# 如果 string 中包含至少一个区分大小写的字符，并且所有这些(区分
# 大小写的)字符都是大写，则返回 True，否则返回 False
# print(la.join(lb))
# 以 string 作为分隔符，将 seq 中所有的元素(的字符串表示)合并为
# 一个新的
# 字符串
#print(la.lower())
#转换 string 中所有大写字符为小写
#print(la.lstrip())
#截掉 string 左边的空格
#print(la.replace('s','D',5))
# 把 string 中的 str1 替换成 str2,如果 num 指定，则替换不超过 num 次.


# print(la.rstrip())
# 删除 string 字符串末尾的空格

# print(la.split('s',la.count(la)))
# 以 str 为分隔符切片 string，如果 num 有指定值，则仅分隔 num+
# 个子字符串
# print(la.startswith('s',0,100))
# 检查字符串是否是以 obj 开头，是则返回 True，否则返回 False。
# 如果beg 和 end 指定值，则在指定范围内检查.

# print(la.strip('    '))
# 在 string 上执行 lstrip()和 rstrip()

# print(la.swapcase())
# 翻转 string 中的大小写
#
# print(la.upper())
# 转换 string 中的小写字母为大写

